[pytest]
addopts = -q --strict-markers -m "not legacy"
markers =
    smoke: quick checks for availability and core UI
    flow: end-to-end user flows
    ui: general UI checks
    legacy: tests kept for reference but excluded from default runs
